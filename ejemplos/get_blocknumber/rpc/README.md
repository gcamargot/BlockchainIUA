# Comunicación con un nodo utilizando HTTP RPC

Estos *scripts* dialogan con un nodo intercambiando mensajes JSON vía HTTP. La única biblioteca utilizada es la necesaria para realizar conexiones HTTP.